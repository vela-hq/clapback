# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files. Everything, not just the two names we happen to use: a
# `vercel env pull` into an unusual filename is one keystroke away, and the
# cost of getting this wrong is a production credential in git history.
.env*
!.env*.example

# typescript
*.tsbuildinfo
next-env.d.ts

# vercel
.vercel

# claude
/.claude/settings.local.json
